<%= render 'users/user', user: @user %>

<% @posts.each_with_index { |post_, index_| %>
  <%= render partial: 'post', locals:{post: post_, index:index_} %>
<% } %>

<% if user_posts_more_than?(@user) %>
  <div class='text-center mt-3'>
  <% (1..@page_amount).each { |i| %>
    <a href='<%= "#{url_for [@user, :posts]}?page=#{i}" %>'><%= i %></a>
  <% } %>
  </div>
<% end %>